# app.finloraq.com is the authenticated product, with one exception:
# "/" is now the public marketing homepage (see src/app/page.tsx) and is
# meant to be indexed. Every other route is behind login or exists only
# to route into one, so there's nothing else here worth a crawl budget.
User-agent: *
Allow: /$
Disallow: /
